<h1><%= t('user.edit.title') %></h1>

<%= render 'form', user: @user %>

<div class="row">
  <div class="col-md-4 col-md-offset-2">
    <%= link_to t('user.delete'), @user, method: :delete, data: {confirm: t('user.delete_confirmation')}, class: "btn danger sharp" %>
  </div>
</div>
